const rootReducer = function (state = {}, action) {
  switch(action.type){
    default:
      return state;
  }
}


// combineReducers({
//   posts: PostsReducer,
//   form: formReducer
// });

export default rootReducer;
